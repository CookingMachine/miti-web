namespace urls {
    export const category = (id: string) => `/category/${id}`;
    export const recipe = (id: number) => `/recipe/${id}`;
    export const adminHome = `/admin`;
}

export default urls;