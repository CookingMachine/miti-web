import * as React from 'react';

interface Props {
    
}

const Header = ({}: Props) => {
    return (
        <div className='header'>
            Header
        </div>
    )
}

export default Header;